{
  "buildCommand": "cd ogsp/frontend && npm install && npm run vercel-build",
  "outputDirectory": "ogsp/frontend/build",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}